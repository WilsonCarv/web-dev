import { HighlightDirective } from './highligth.directive';

describe('HighligthDirective', () => {
	it('should create an instance', () => {
		const directive = new HighlightDirective({} as any);
		expect(directive).toBeTruthy();
	});
});
